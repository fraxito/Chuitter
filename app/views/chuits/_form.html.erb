<%= form_for(@chuit) do |f| %>
  <% if @chuit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chuit.errors.count, "error") %> prohibited this chuit from being saved:</h2>

      <ul>
      <% @chuit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_field :text %>
  </div>
  <div class="field">
    <%= f.label :usuario_id %><br>
    <%= f.number_field :usuario_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
